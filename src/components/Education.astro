<section id="education" class="education-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Educación</h2>
      <div class="section-subtitle">Mi formación académica</div>
    </div>
    
    <div class="education-notification">
      <div class="notification-header">
        <div class="notification-dots">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
        </div>
        <span class="notification-title">EDUCATION_STATUS</span>
      </div>
      <div class="notification-content">
        <span class="terminal-prompt">$</span>
        <span class="terminal-command">cat education.txt</span>
        <div class="terminal-output">
          <span class="output-line">[INFO] Institution: Universidad Adventista del Plata</span>
          <span class="output-line">[DEGREE] Ingeniería en Sistemas de Información</span>
          <span class="output-line">[PERIOD] Marzo 2021 - Presente</span>
          <span class="output-line">[STATUS] En curso</span>
          <span class="output-line">[PROGRESS] ████████████████████ 95%</span>
          <span class="output-line"></span>
          <span class="output-line">[COMPLETED] Analista en Sistemas de Información</span>
          <span class="output-line">[PERIOD] Marzo 2021 - Diciembre 2023</span>
          <span class="output-line">[STATUS] Finalizado ✓</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .education-section {
    padding: 4rem 0;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.02), rgba(0, 255, 136, 0.02));
    position: relative;
    overflow: hidden;
  }

  .education-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
      linear-gradient(rgba(0, 255, 136, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 255, 136, 0.03) 1px, transparent 1px);
    background-size: 100px 100px;
    opacity: 0.5;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 2;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    color: #00ff41;
    font-family: 'Courier New', monospace;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin: 0 0 1rem 0;
    text-shadow: 0 0 10px rgba(0, 255, 65, 0.3);
  }

  .section-subtitle {
    font-size: 1.125rem;
    color: #9ca3af;
    font-weight: 500;
  }

  .education-notification {
    max-width: 600px;
    margin: 0 auto;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.9), rgba(15, 23, 42, 0.8));
    border: 1px solid rgba(0, 255, 136, 0.4);
    border-radius: 12px;
    backdrop-filter: blur(10px);
    overflow: hidden;
    position: relative;
    box-shadow: 
      0 0 30px rgba(0, 255, 136, 0.2),
      inset 0 0 30px rgba(0, 255, 136, 0.05);
    transition: all 0.3s ease;
  }

  .education-notification:hover {
    transform: translateY(-5px);
    box-shadow: 
      0 10px 40px rgba(0, 255, 136, 0.3),
      inset 0 0 30px rgba(0, 255, 136, 0.1);
  }

  .education-notification::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(0, 255, 136, 0.8), transparent);
    animation: scan-line 3s ease-in-out infinite;
  }

  @keyframes scan-line {
    0%, 100% { opacity: 0; }
    50% { opacity: 1; }
  }

  .notification-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.5rem;
    background: rgba(0, 255, 136, 0.1);
    border-bottom: 1px solid rgba(0, 255, 136, 0.3);
  }

  .notification-dots {
    display: flex;
    gap: 0.5rem;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 1px solid currentColor;
  }

  .dot.red { background: #ef4444; border-color: #dc2626; }
  .dot.yellow { background: #f59e0b; border-color: #d97706; }
  .dot.green { background: #10b981; border-color: #059669; }

  .notification-title {
    font-family: 'Courier New', monospace;
    font-size: 0.875rem;
    color: #00ff88;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .notification-content {
    padding: 1.5rem;
    font-family: 'Courier New', monospace;
    font-size: 1rem;
    line-height: 1.6;
  }

  .terminal-prompt {
    color: #00ff88;
    font-weight: bold;
    margin-right: 0.5rem;
  }

  .terminal-command {
    color: #ffffff;
    font-weight: 500;
  }

  .terminal-output {
    margin-top: 1rem;
    padding-left: 1rem;
  }

  .output-line {
    display: block;
    color: #00ff88;
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
  }

  .output-line:last-child {
    margin-bottom: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .education-section {
      padding: 3rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .education-notification {
      margin: 0 1rem;
    }

    .notification-content {
      padding: 1rem;
      font-size: 0.875rem;
    }

    .output-line {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 480px) {
    .education-section {
      padding: 2rem 0;
    }

    .container {
      padding: 0 0.5rem;
    }

    .education-notification {
      margin: 0 0.5rem;
    }

    .notification-content {
      padding: 0.75rem;
      font-size: 0.75rem;
    }

    .output-line {
      font-size: 0.7rem;
    }
  }
</style> 