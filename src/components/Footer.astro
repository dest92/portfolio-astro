---
import NavLink from "./NavLink.astro";
import { useTranslations } from "../i18n/utils";
import { ui } from "../i18n/ui";
import { getRelativeLocaleUrl } from "astro:i18n";

const lang = (Astro.currentLocale || "es") as keyof typeof ui;
const t = useTranslations(lang);
---

<footer class="mt-32 flex-none">
  <div class="sm:px-8">
    <div class="mx-auto w-full max-w-7xl lg:px-8">
      <div class="border-t border-zinc-100 pb-16 pt-10 dark:border-zinc-700/40">
        <div class="relative px-4 sm:px-8 lg:px-12">
          <div class="mx-auto max-w-2xl lg:max-w-5xl">
            <div
              class="flex flex-col items-center justify-between gap-6 sm:flex-row"
            >
              <div
                class="flex flex-wrap justify-center gap-x-6 gap-y-1 text-sm font-medium text-zinc-800 dark:text-zinc-200"
              >
                <NavLink href={getRelativeLocaleUrl(lang, "/")}
                  >{t("nav.home")}</NavLink
                >
                <NavLink href={getRelativeLocaleUrl(lang, "#experience")}
                  >{t("nav.experience")}</NavLink
                >
                <NavLink href={getRelativeLocaleUrl(lang, "#proyectos")}
                  >{t("nav.projects")}</NavLink
                >
              </div>
              <p class="text-sm text-zinc-400 dark:text-zinc-500">
                &copy; {new Date().getFullYear()} Matías Acebal. {
                  t("footer.rights")
                }
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</footer>
